package com.isay.tool;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Check {
	//ÅÐ¶Ï×Ö·û´®ÖÐÊÇ·ñº¬ÓÐ×ÖÄ¸
    public static boolean checkAZ(String cardNum) {  
        String regex=".*[a-zA-Z]+.*";  
        Matcher m=Pattern.compile(regex).matcher(cardNum);  
        return m.matches();  
    } 
}
